import { Height } from "@mui/icons-material";

const NotFound = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1 style={{ fontSize: "32px" }}>404 - Expected page not found</h1>
    </div>
  );
};

export default NotFound;
